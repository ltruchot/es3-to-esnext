// the language that no one learn
// c/p a lot of stuff, relay on stackoverflow

// then misunderstanding leads to hate & language blaming
if (a === 1 && a === 2 && a === 3) {
  // prototype
  console.log("JavaScript is fucked up");
}
if ([] + [] === "") {
  // coercion
  console.log("WTF JavaScript...");
}
if (NaN !== NaN) {
  // decision
  console.log("My weird cousin JavaScript");
}
if (typeof null === "object") {
  // real lack
  console.log("OK, sometimes, it's true");
}

// a brief history of JS

// scope, compiler & runtime + LHS/RHS

// eventloop

// scope -> closure -> modules

// native types and their constructors + TypeError

// coercion & equality

// undefined vs undeclared + ReferenceError

// use strict, eval & with
